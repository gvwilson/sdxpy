readability -- vs -> capability
static site generator -- can use -> Visitor pattern
{data, page templates} -- combine to make -> HTML pages
template language -- has -> {conditionals, constants, loops, variables}
