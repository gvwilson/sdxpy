dynamic dispatch -- relates to -> uniform signature
persistence -- must be -> extensible
extensible -- to handle -> user classes
extensible -- should respect -> Open-Closed Principle
extensible -- via -> dynamic dispatch
dynamic dispatch -- needs -> uniform signature
persistence -- for -> {atomic values, collections, user classes}
{collections, user classes} -- can have -> aliasing
