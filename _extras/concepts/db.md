blocks -- contain a fixed number of -> records
records -- have -> keys
index -- references -> keys
database -- has -> index
database -- has -> blocks
index -- all stored in -> memory
blocks -- some stored in -> memory
blocks -- all stored in -> filesystem
