terminal application -- uses -> headless screen
buffer -- stores -> text
cursor -- position in -> buffer
cursor -- displayed in -> window
text -- displayed in -> window
headless screen -- replaces -> curses screen
action objects -- saved as -> history
terminal application -- constructs -> {action objects, buffer, cursor, window}
