class -- defines -> methods
object -- has -> attributes
object -- instance of -> class
attributes -- stored in -> dictionary
dictionary -- refers to -> dictionary
methods -- includes -> constructor
{class, methods} -- respects -> contract
methods -- stored in -> {constructor, dictionary}
