terminal application -- uses -> curses
curses -- creates -> screen
screen -- passed to -> terminal application
buffer -- stores -> text
cursor -- position in -> buffer
cursor -- displayed in -> window
text -- displayed in -> window
terminal application -- constructs -> {buffer, cursor, window}
