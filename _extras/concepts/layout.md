layout engine -- uses -> coordinate system
layout engine -- handles -> rendering
rendering -- using -> recursion
rendering -- of -> blocks
row -- can -> wrap
blocks -- stored as -> tree
recursion -- to traverse -> tree
blocks -- have -> position
position -- in -> coordinate system
position -- calculate -> {column, row}
