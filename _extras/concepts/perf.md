dataframe -- supports -> operations
select -- keep -> columns
filter -- keep -> rows
filter -- slow -> column-wise
filter -- fast -> row-wise
select -- fast -> column-wise
select -- slow -> row-wise
experiments -- help design -> operations
dataframe -- stored -> {column-wise, row-wise}
dataframe -- has -> {columns, rows}
operations -- include -> {filter, select}
