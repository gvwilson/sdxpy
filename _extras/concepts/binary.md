bits -- written as -> hexadecimal
bits -- manipulated with -> bitwise operations
bitwise operations -- relates to -> left shift
bitwise operations -- relates to -> right shift
bitwise operations -- relates to -> and
bitwise operations -- relates to -> or
bitwise operations -- relates to -> xor
bitwise operations -- relates to -> not
boxed value -- usually -> scattered
values -- made of -> bits
bits -- interpeted as -> values
bare value -- can be -> contiguous
boxed value -- pack -> bare value
bare value -- unpack -> boxed value
integer -- stored as -> {sign and magnitude, two's complement}
{UTF-32, UTF-8} -- are -> Unicode
values -- can be -> {character, integer}
