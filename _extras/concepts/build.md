build manager -- updates -> files
build manager -- obeys -> build rules
dependencies -- can be -> stale
dependencies -- form a -> directed acyclic graph
directed acyclic graph -- sorted in -> topological order
stale -- compared to -> target
recipe -- updates -> target
dependencies -- are -> files
target -- are -> files
topological order -- example of -> graph algorithm
build rules -- consist of -> {dependencies, recipe, target}
