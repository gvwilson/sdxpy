package manager -- uses -> theorem prover
theorem prover -- to find -> compatible
packages -- installed by -> package manager
packages -- installation requires -> compatible
compatible -- sets of -> dependencies
packages -- have -> dependencies
dependencies -- are -> packages
packages -- have -> versions
semantic version numbers -- have -> {major, minor, patch}
