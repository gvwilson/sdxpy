runtime -- uses -> recursive evaluation
runtime -- stores variables in -> environments
functions -- found by -> introspection
environments -- many -> local environment
environments -- one -> global environment
recursive evaluation -- to -> dispatch
dispatch -- simple -> functions
introspection -- makes systems -> extensible
functions -- can be -> {eager, lazy}
